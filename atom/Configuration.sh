sudo apm install atom-beautify;
sudo apm install atom-latex;
sudo apm install build;
sudo apm install busy-signal;
sudo apm install docblockr;
sudo apm install emmet;
sudo apm install file-icons;
sudo apm install goto-definition;
sudo apm install multi-cursor;
sudo apm install pdf-view;
sudo apm install pigments;
sudo apm install language-latex;
sudo apm install latex;
sudo apm install latex-autocomplete;
sudo apm install latextools;
sudo apm install minimap;
sudo apm install spacegray-atom-dark-syntax;

cp ./atom/config.cson ~/.atom/;
